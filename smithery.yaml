# Generated by `pnpm gen:agent-skill` from packages/agent-skills/src/repo.ts.
# Format: https://smithery.ai/docs/build/session-config
version: 1
startCommand:
  type: http
  url: https://api.agentisend.com/mcp
  configSchema:
    type: object
    properties:
      apiKey:
        type: string
        title: "AgentiSend API key"
        description: "Create one in the console at https://console.agentisend.com; the key is sent as a bearer token."
        x-from:
          header: apiKey
        x-to:
          header: Authorization
  exampleConfig:
    apiKey: as_0000000000000000000000000000000000000000
